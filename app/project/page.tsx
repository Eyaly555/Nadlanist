/**
 * עמוד ריק עם כותרת עליונה ותחתונה בלבד
 * @returns {JSX.Element} עמוד ריק (רק Header + Footer)
 */
export default function ProjectEmptyPage() {
  return (
    <main className="flex-1" dir="rtl" />
  );
} 